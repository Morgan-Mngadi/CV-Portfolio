{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "PORT=5173 BASE_PATH=/ pnpm --filter @workspace/portfolio dev",
    "dev:portfolio": "PORT=5173 BASE_PATH=/ pnpm --filter @workspace/portfolio dev",
    "preview": "PORT=4173 BASE_PATH=/ pnpm --filter @workspace/portfolio serve",
    "build:portfolio": "PORT=5173 BASE_PATH=/ pnpm --filter @workspace/portfolio build",
    "build:portfolio:ssr": "PORT=5173 BASE_PATH=/ pnpm --filter @workspace/portfolio build:ssr",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
